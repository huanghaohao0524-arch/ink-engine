# 墨引擎产品说明

墨引擎是一个本地优先的 AI 网文写作工作台实验版。

它关注的不是“一键生成小说”，而是把作者真实写作过程中容易断开的环节串起来：从一个脑洞开始，逐步形成项目资料、平台规则、设定、大纲、章节草稿、修订候选和追踪记录。

## 产品定位

墨引擎面向想用 AI 辅助中文网文创作的人。

它的核心目标是：

- 帮作者把模糊想法整理成可继续推进的书籍项目。
- 在章节写作时保留平台规则、设定、角色和上下文。
- 让 AI 输出先进入候选区，避免直接覆盖用户正文。
- 使用本地文件保存项目，尽量降低数据迁移和工具锁定成本。

## 当前实验能力

当前版本可以试玩这些方向：

1. 选择本地写作库。
2. 扫描已有书籍目录。
3. 创建书籍项目。
4. 配置 OpenAI 兼容接口。
5. 进入章节编辑器。
6. 生成章节候选。
7. 检查、修订和应用候选内容。
8. 通过本地文件保存正文和项目资料。

## 写作工作流

墨引擎的基础写作流程是：

```mermaid
flowchart LR
  A["一句脑洞"] --> B["立项问答"]
  B --> C["生成项目资料"]
  C --> D["章节准备"]
  D --> E["生成正文候选"]
  E --> F["人工检查与修订"]
  F --> G["应用到正文"]
  G --> H["同步追踪记录"]
  H --> I["继续下一章"]
```

其中最重要的设计原则是：

- AI 生成内容默认只是候选。
- 用户确认后，候选才会写入正文或项目资料。
- 项目资料和正文分开保存，避免设定、草稿和最终文本混在一起。

## 本地项目结构

墨引擎倾向于使用透明的本地文件结构。

一个书籍项目通常会包含：

```text
书名/
  book.json
  设定/
  角色/
  大纲/
  正文/
  追踪/
  导出/
  .backup/
```

`正文/` 下的章节文件是主要写作内容。

`设定/`、`角色/`、`大纲/`、`追踪/` 用于保存 AI 写作时需要参考和更新的项目记忆。

## AI 接口

当前版本使用用户自己的 OpenAI 兼容接口配置。

应用内可配置：

- API Key
- Base URL
- 模型名称
- 代理地址

API Key 保存在本机配置里，不应该提交到仓库。

## 当前技术状态

这个仓库是一个已经开放出来的实验版本。

需要特别说明：

- 当前可运行界面主要来自恢复后的 `public/legacy-runtime.js`。
- `src/App.tsx` 保留了恢复期源码契约和测试锚点。
- 后续如果继续维护，优先工作应是重建可维护的 React 源码入口，而不是继续堆功能。

## 适合贡献的方向

欢迎围绕这些方向继续改造：

1. 重建可维护的 React 前端源码。
2. 拆分 Electron 主进程里的 AI、文件和写作流程逻辑。
3. 补充真实端到端用户路径测试。
4. 改善本地写作项目的导入、保存和备份体验。
5. 优化候选区、正文区和追踪同步之间的交互。

## 非目标

墨引擎不是平台后台，也不是自动发布工具。

它当前更适合作为一个本地写作实验项目，而不是完整商业产品。
